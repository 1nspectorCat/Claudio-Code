<?xml version="1.0" encoding="utf-8"?>
<adaptive-icon xmlns:android="http://schemas.android.com/apk/res/android">
    <background android:drawable="@color/ic_bg" />
    <foreground android:drawable="@drawable/ic_spark" />
    <!-- Themed icons (Android 13+) use the alpha channel only, so the colored
         spark doubles as the monochrome layer. -->
    <monochrome android:drawable="@drawable/ic_spark" />
</adaptive-icon>
